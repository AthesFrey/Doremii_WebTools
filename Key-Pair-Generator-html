<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <title>Base64URL-X25519-KEYS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- 依赖：本地托管的 TweetNaCl（必须先加载） -->
  <script src="/wp-content/uploads/tweetnacl-fast-1.0.3.min.js"></script>

  <!-- 业务脚本：x25519-keygen.js（不改 JS，仅改 HTML/CSS） -->
  <script defer src="/wp-content/uploads/x25519-keygen.js?v=20251218B5"></script>

  <style>
    :root{
      /* 橘色系（保留你原来的审美，但把结构简化） */
      --accent:#ff7a00;
      --accent-soft: rgba(255,122,0,.28);
      --bg:#fffaf2;
      --card:#ffffff;
      --text:#1f2328;
      --muted:#6b7280;
      --mono-bg:#fff2df;

      --radius:18px;
      --shadow:0 8px 26px rgba(0,0,0,.08);
      --btn-h:38px;

      --maxw:520px; /* ✅ 默认宽度：520px */
      --font: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial, "Noto Sans", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
      --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace;
    }

    body{
      margin:0;
      padding:0;
      background:var(--bg);
      color:var(--text);
      font-family:var(--font);
    }

    /* ✅ 居中方式简化：一个容器 + margin auto */
    .tool-center{
      max-width:var(--maxw);
      margin:0 auto;
      padding:10px 12px 18px;
      box-sizing:border-box;
    }

    h2{
      margin:0 0 10px;
      font-size:20px;
      font-weight:800;
      text-align:center;
      letter-spacing:.2px;
    }

    .card{
      background:var(--card);
      border:1px solid rgba(0,0,0,.04);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      padding:18px 18px 16px;
      box-sizing:border-box;
      width:100%; /* ✅ 填满 520 容器 */
    }

    .sub{
      font-size:13px;
      line-height:1.55;
      color:var(--muted);
      margin:6px 0 12px;
      text-align:center;
    }

    .row{
      display:flex;
      align-items:center;
      gap:8px;
      margin-top:8px;
      flex-wrap:wrap;
    }

    .label{
      min-width:92px;
      font-size:13px;
      color:var(--muted);
    }

    .mono{ font-family:var(--mono); }

    .result{
      flex:1 1 260px;
      min-height:var(--btn-h);
      background:var(--mono-bg);
      border-radius:999px;
      padding:6px 12px;
      box-sizing:border-box;
      display:flex;
      align-items:center;
      word-break:break-all;
      font-size:13px;
      line-height:1.3;
    }

    .input{
      flex:1 1 260px;
      height:var(--btn-h);
      background:var(--mono-bg);
      border-radius:999px;
      padding:0 12px;
      border:1px solid var(--accent-soft);
      box-sizing:border-box;
      outline:none;
      font-family:var(--mono);
      font-size:13px;
    }
    .input:focus{ border-color:var(--accent); }

    button{
      height:var(--btn-h);
      padding:0 14px;
      border-radius:999px;
      border:1px solid transparent;
      font-size:13px;
      cursor:pointer;
      user-select:none;
      transition:.12s ease;
      white-space:nowrap;
    }

    .btn-main{
      background:var(--accent);
      color:#fff;
      font-weight:700;
      box-shadow:0 6px 18px rgba(255,122,0,.22);
    }
    .btn-main:hover{ filter:brightness(1.05); transform:translateY(-0.5px); }
    .btn-main:active{ filter:brightness(.97); transform:translateY(0); }

    .btn-ghost{
      background:#fff7eb;
      border-color:var(--accent-soft);
      color:#ff8a00;
      font-weight:700;
    }
    .btn-ghost:hover{ filter:brightness(1.03); transform:translateY(-0.5px); }
    .btn-ghost:active{ filter:brightness(.98); transform:translateY(0); }

    .hist{
      margin-top:10px;
      background: rgba(255,122,0,.06);
      border:1px solid rgba(255,122,0,.16);
      border-radius:14px;
      padding:10px;
      box-sizing:border-box;
    }

    .hist-row{
      display:flex;
      gap:10px;
      align-items:flex-start;
      padding:6px 2px;
      border-bottom:1px dashed rgba(0,0,0,.08);
    }
    .hist-row:last-child{ border-bottom:none; }

    .hist-index{
      width:34px;
      color:var(--muted);
      font-size:12px;
      line-height:1.35;
      padding-top:1px;
      flex:0 0 auto;
    }

    .hist-text{
      flex:1;
      font-family:var(--mono);
      font-size:12px;
      line-height:1.35;
      word-break:break-all;
      color:#222;
    }

    .hist-copy{
      height:28px;
      padding:0 10px;
      font-size:12px;
      border-radius:999px;
      background:#fff;
      border:1px solid rgba(0,0,0,.08);
    }
    .hist-copy:hover{ transform:translateY(-0.5px); }

    @media (max-width:520px){
      .label{ min-width:76px; }
    }
  </style>
</head>

<body>
  <div class="tool-center">
    <h2>Base64URL-X25519-密钥生成器</h2>

    <div class="card" id="x25519-card">
      <div class="sub">
        私钥：Base64URL（43 字符），公钥使用 X25519（<code>nacl.scalarMult.base</code>）推导。
      </div>

      <!-- 保持 JS 依赖的 id 不变 -->
      <div class="row">
        <div class="label">操作</div>
        <button class="btn-main" id="x-generate">Generate</button>
      </div>

      <div class="row">
        <div class="label">私钥→公钥</div>
        <input
          class="input mono"
          id="x-priv-input"
          type="text"
          inputmode="text"
          autocomplete="off"
          autocapitalize="off"
          spellcheck="false"
          placeholder="粘贴 43 字符 Base64URL 私钥（sk）"
        />
        <button class="btn-ghost" id="x-calc-pub">Calc pk</button>
      </div>

      <div class="row">
        <div class="label">Private key</div>
        <div class="result mono" id="x-priv"></div>
        <button class="btn-ghost" id="x-copy-priv">Copy</button>
      </div>

      <div class="row">
        <div class="label">Public key</div>
        <div class="result mono" id="x-pub"></div>
        <button class="btn-ghost" id="x-copy-pub">Copy</button>
      </div>

      <div class="row" style="margin-top:12px;">
        <div class="label">results:</div>
      </div>
      <div class="hist" id="x-hist"></div>
    </div>
  </div>
</body>
</html>
